const { getIdentityStatus } = require('./get-identity-status');
const { getPendingAuthorizations } = require('./get-pending-authorizations');

module.exports = {
  getIdentityStatus,
  getPendingAuthorizations,
};