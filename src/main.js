const onchain = require('./onchain');
const offchain = require('./offchain');
const { submitTx } = require('./utils');

(async () => {
  /* sandbox, you can play around here using the examples in the README */
})().catch(err => console.error(err));